# GitHubで表示するaboutを設定する

## 目的
Githubで表示するaboutを設定していない。HomebrewのFormulaとして公開する際にDescriptionとして使えると思うので考えて欲しい。

## 要望
このプロダクトのaboutを考えてください

## 完了サマリー

- 完了日時: 2026-05-19T13:40:11+09:00
- **採用テキスト**: `AWS AssumeRole CLI tool with TOTP-based MFA support and flexible credential output`
- **変更内容**:
  - `Cargo.toml` の `description` フィールドを更新
  - GitHub リポジトリの about を `gh repo edit` で設定（確認済み）
